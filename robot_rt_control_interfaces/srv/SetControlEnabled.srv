# R-IN-03 /control/set_enabled。维护/生命周期工具 → RT-Control。
#
# 公共启停入口。enabled=true 走 reset/enable 适配逻辑；
# enabled=false 只调用底层 disable，不停止 launch/container。
# 不复位急停、安全继电器或 STO；不属于箱级任务流程，Autonomy 不得调用。

bool enabled

# 调用原因，仅用于日志追溯。
string reason
---
bool accepted

# 操作后的实际使能状态。
bool enabled

# 失败原因。accepted=true 时 code 必须为 SUCCESS。
# 取值见 robot_system_interfaces/msg/ErrorCode.msg 的 1xxx 段。
robot_system_interfaces/ErrorInfo error
