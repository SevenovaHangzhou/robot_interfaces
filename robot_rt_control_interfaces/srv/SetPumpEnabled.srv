# R-IN-04 /vacuum/pump/set_enabled。维护工具或 RT 内部管理器 → RT-Control。
#
# 真空泵维护接口。活动真空命令、PLC 不新鲜、任一通道 attached 或阀仍开时
# 拒绝普通停泵。Autonomy 不得调用；活动 G-01 期间调用 enabled=false 的次数必须为零。

bool enabled

# 调用原因，仅用于日志追溯。
string reason
---
bool accepted

# 操作后的实际泵状态。
bool enabled

# 失败原因。accepted=true 时 code 必须为 SUCCESS。
# 取值见 robot_system_interfaces/msg/ErrorCode.msg 的 1xxx 段。
robot_system_interfaces/ErrorInfo error
