# 一个取放序列。双箱序列共用同一个 Goal，是不可拆分的整体。
# 全部字段由 P-01 一次产出，下游只消费不重算。

# 序列序号，从 0 起，全计划内唯一且连续。
uint16 sequence_index

# 本序列的 1～2 个箱。双箱任一侧失败则整个序列不 Commit。
BoxPose[] boxes

# 本序列对应的工位标识。同一工位可对应多个序列。
string station_id

# 本序列所属的箱墙排号。**0 = 最下排，向上递增。**
# 感知事实：由箱墙结构识别得到，决定退避距离的取值。
#
# 约束：本序列的箱不得跨越退避距离不同的排（当前即不得跨 1|2 边界）。
# 同退避组内是否跨排为待定项，但跨组一定不允许 —— 否则单个
# station_standoff_m 无法表达本序列的退避距离。
uint8 wall_row

# 本序列实际使用的退避距离，单位 m。
#
# 由运控退避表按 wall_row 查得（见契约 2.1）。Perception 填写**实际使用的值**，
# 不填标称值。消费方不据此重算位姿，仅用于诊断与配置一致性核对 ——
# 该值与当前退避表不符即说明感知侧配置过期。
float32 station_standoff_m

# 可直接执行的到站导航位姿，map 坐标系，非空。
#
# 导出规则（运控设计，可达性已在设计阶段编码进退避表）：
#   取本序列所属列的箱墙面心 → 法向量与 XY 方向平均值
#   → 沿 X 方向回退 station_standoff_m
#
# Motion 的 Nav2 只消费本位姿，不解析语义地图、不重算、不自行加偏置。
geometry_msgs/PoseStamped station_nav_pose

# 本序列的近距离重拍位，按臂绑定，1～2 项，与 boxes 同集同序。
# M-01 直接消费，不重新解算。
CameraViewPose[] camera_view_poses
